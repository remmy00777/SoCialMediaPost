import { DataPage } from "@/components/DataPage";
export default function Page() { return <DataPage title="Trend Detail" description="Select a candidate ID from the trend explorer and inspect its normalized source data, metric history, score explanation, structured analysis, and concept audit trail through the API." endpoint="/api/trends" />; }
