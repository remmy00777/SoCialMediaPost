import { DataPage } from "@/components/DataPage";
export default function Page() { return <DataPage title="Published Content" description="View real platform posts and explicitly labeled simulated publications, along with normalized analytics snapshots and unsupported-metric status codes." endpoint="/api/analytics/overview" />; }
