import { DataPage } from "@/components/DataPage";
export default function Page() { return <DataPage title="Notifications" description="Review account, token, workflow, publication, originality, policy, storage, backup, quota, outage, and scheduler notifications." endpoint="/api/notifications" />; }
