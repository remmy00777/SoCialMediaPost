import { DataPage } from "@/components/DataPage";
export default function Page() { return <DataPage title="Content Rules" description="Inspect enforced rules for original scripts and assets, rights evidence, policy safety, disclosure, factual review, and blocking originality thresholds." endpoint="/api/security/status" />; }
