import { DataPage } from "@/components/DataPage";
export default function Page() { return <DataPage title="Experiments" description="Manage versioned hypotheses, controls, variants, target and guardrail metrics, sample requirements, confidence thresholds, decisions, and rollback versions." endpoint="/api/experiments" actions={[{ label: "Create Demo Experiment", path: "/api/experiments", body: { name: "Hook framework test", hypothesis: "A three-question hook improves completion without increasing negative feedback.", control: { hook: "single claim" }, variant: { hook: "three questions" }, target_metric: "completion_rate", guardrail_metrics: ["negative_feedback_rate", "unfollow_rate"], minimum_sample_size: 30, confidence_requirement: 0.95 } }]} />; }
