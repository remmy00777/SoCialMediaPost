import { DataPage } from "@/components/DataPage";
export default function Page() { return <DataPage title="Provider Configuration" description="Configure replaceable LLM, TTS, STT, image, video, stock-media, music, moderation, and embedding providers. Local fallbacks support basic operation without paid APIs." endpoint="/api/providers" />; }
