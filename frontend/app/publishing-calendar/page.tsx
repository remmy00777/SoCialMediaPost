import { DataPage } from "@/components/DataPage";
export default function Page() { return <DataPage title="Publishing Calendar" description="Review configurable discovery and production schedules, scheduling windows, and queued publication work. America/Chicago is the default timezone." endpoint="/api/schedules" />; }
