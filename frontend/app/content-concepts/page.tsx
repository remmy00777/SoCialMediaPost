import { DataPage } from "@/components/DataPage";
export default function Page() { return <DataPage title="Content Concepts" description="Review selected and rejected concept candidates, their component scores, hypotheses, confidence, failure risks, and prompt versions." endpoint="/api/content-packages" />; }
